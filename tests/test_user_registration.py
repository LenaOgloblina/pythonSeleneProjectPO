from pythonSeleneProjectPO.data.users import User
from pythonSeleneProjectPO.pages.simple_user_registration_page import simpleUserRegistrationPage


def test_registration_user():
    pass
    # registration_page = simpleUserRegistrationPage()
    # lena = User(full_name='Elena Ivanova',
    #             email='test@test.com',
    #             currentAddress='123456, Lenina str., 55-16',
    #             permanentAddress='321654, Mira str., 66-12')
    #
    # registration_page.open()
    # registration_page.full_name(lena.full_name)
    # registration_page.email(lena.email)
    # registration_page.currentAddress(lena.currentAddress)
    # registration_page.permanentAddress(lena.permanentAddress)
    # registration_page.submit().click()

    # registration_page.should_have_submited(lena.full_name,
    #                                        lena.email,
    #                                        lena.currentAddress,
    #                                        lena.permanentAddress)